apiVersion: 1.33.3-gke.1136000
kind: Pod
metadata:
  name: my-app-pod
  labels:
    app: web-server
spec:
  containers:
  - name: my-app-container
    image: nagamalleswarrao/abctechnologies:12

---

apiVersion: 1.33.3-gke.1136000
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: web-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP



